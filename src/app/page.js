import React from 'react'
import { meridian } from './layout'

const page = () => {
  return (
    <div>
      <h1 className={`${meridian.className} text-red-600`}>Main page</h1>
    </div>
  )
}

export default page